def gcd(a, b) #обчислює найбільший спільний дільник двох чисел a та b за допомогою алгоритму Евкліда.
  while b != 0
    a, b = b, a % b
  end
  a
end

puts "Введіть перше число:"
a = gets.to_i
puts "Введіть друге число:"
b = gets.to_i

puts "НСД для чисел #{a} і #{b} дорівнює #{gcd(a, b)}"
